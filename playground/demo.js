// Const a = [1,2,3,10]

// Const b = [7,8,9]

const a = [1, 2, 3, 10];
const b = [6, 7, 8];

// Find the index of the element '10' in array 'a'
const index = a.indexOf(10);
console.log(a.unshift(...b))

// Use the splice method to insert the elements of array 'b' after index 2 and before index 'index'
// a.splice(3, 0, ...b);

console.log(a);


